[package]
name = "example-hello-world-rs"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"

[dependencies]
resonate = { package = "resonate-sdk", version = "0.6.0" }
tokio = { version = "1", features = ["full"] }
serde = { version = "1", features = ["derive"] }  # Required by resonate macro for argument serialization
